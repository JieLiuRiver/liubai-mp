.container {
  height: 100%;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  box-sizing: border-box;
  font-family: "PingFang SC", "Helvetica Neue", Helvetica, Arial, "Hiragino Sans GB", "Microsoft Yahei", ΢���ź�, STHeiti, ����ϸ��, sans-serif;
}


@keyframes bounce{
  from{transform: translate3d(0,16rpx,0);}
  to{transform: translate3d(0,0,0);}
}

.slide-up{animation:slideUp ease .3s forwards}
@keyframes slideDown{from{transform:translate3d(0,0,0)}
to{transform:translate3d(0,100%,0)}
}